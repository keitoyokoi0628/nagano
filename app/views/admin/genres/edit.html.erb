<%= form_with model: @genre, url: admin_genre_path(@genre.id), method: :patch do |f| %>
<div class="container">
 <h4 class="text-left p-5">ジャンル編集</h4>
 <div class="row">


   <p>ジャンル名　　　<%= f.text_field :name %>　<%= f.submit '変更を保存', class: "btn btn-success" %></p>

  <% end %>
 </div>
</div>