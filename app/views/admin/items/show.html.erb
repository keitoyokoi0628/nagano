<h4 class="text-left p-5">商品詳細</h4>
<div class="container">
 <div class="row">
  <div class="col-3">
    <% if @item.image.attached? %>
      <%= image_tag @item.image, size: "200x200" %>
    <% else %>
      <%= image_tag 'no_image', size: "200x200" %>
    <% end %>
  </div>
  <div class="col-9">
    <p>商品名　　　　　　　　　　　<%= @item.name %></p>

    <p>商品説明　　　　　　　　　　<%= @item.introduction %></p>

    <p>ジャンル　　　　　　　　　　<%= @item.genre.name %></p>

    <p>税込価格<br>(税抜価格)　　　　　　　　　<%= @item.add_tax_price.to_s(:dalimited) %>(<%= @item.price %>)円<p>

    <p>販売ステータス<a>　　　　　　　</a>
      <% if @item.is_active %>
        <td>販売中</td>
      <% else %>
       <td>販売停止中</td>
      <% end %></p>

    <p><%= link_to "編集する", edit_admin_item_path(@item.id), class: "btn btn-success" %></p>
  </div>
 </div>
</div>